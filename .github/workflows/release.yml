name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/build.yml

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: executable

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          files: thaumcraft4-research-bot-windows.zip
          generate_release_notes: true